//use tauri_build::{try_build, Attributes, WindowsAttributes};

fn main() {
    // tauri_build::build()
    //   let attrs = WindowsAttributes::new().window_icon_path("../icons/icon.ico");
    //   if let Err(error) = try_build(Attributes::new().windows_attributes(attrs)) {
    //     panic!("error found during tauri-build: {error}",);
    //   }
}
